<header class="navbar navbar-fixed-top navbar-inverse">
      <div class="navbar-inner">
        <div class="container">
            <h3><strong>Social Media Reviews Harvesting</strong</h3>
            <br />
	    <div class="nav pull-right" %>
            <% if login_signed_in? %>
                  <div class="nav pull-right" %>
                      Signed-In as <strong><%=current_login.username %></strong>.
                  </div>
            <% end %>
           </div>
           <nav>
                <ul class="nav nav-tabs" role="tablist">
                   <li class="active"><%= link_to 'Home', root_path %><li>
                   <% if login_signed_in? && current_login.username != 'admin' %>
                   	<li class="dropdown">
                          <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                             Administrative<span class="caret"></span>
                          </a>
                          <ul class="dropdown-menu" role="menu">
                             <li><%= link_to 'Edit profile', edit_login_registration_path %><br /> </li>
                  	     <li class="divider"></li>
			     <li><%= link_to 'Business',  businesses_path %></li>
                   	     <li class="divider"></li>
                             <li><%= link_to 'Social Media site', socialmediasites_path %></li>
                             <li class="divider"></li>
			     <li><%= link_to 'Social Media Response Mapping', ssdmappings_path %></li>
			     
		          <% else %>
                  	 <li><%= link_to "Administrator Sign In", new_login_session_path %></li>
                  <% end %>
                   	<li><%= link_to "Logout", destroy_login_session_path, :method => :delete %></li>
              </ul>
          </nav>
      </div>
    </div>
  </header>
