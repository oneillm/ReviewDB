<% title "Welcome to Collection and Integration of Postings on Social Media - Adminstrative Application" %>
<div class="container"> 
<div class="center hero-unit">
	<h2> <%= yield(:title) %></h2>
           <% if login_signed_in? %>
           <% else %>
        	<%= link_to "First Time Sign up!", 'new_session_path', class: "btn btn-large btn-primary" %>
           <% end %>
 </div>           
</div>
